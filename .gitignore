# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Node (se in futuro aggiungi tooling)
node_modules/
.npm
.pnpm-store/

# Build output
dist/
build/
.cache/

# Credenziali e segreti locali
# ----------------------------------------
# .env contiene i token Cloudflare, GitHub, ecc. usati dagli script locali
# (es. wrangler per il deploy). NON deve mai essere committato.
.env
.env.local
.env.*.local
*.pem
*.key

# Log e file temporanei
*.log

# File generati temporaneamente nella root (es. qrcode.png di prova)
# Non rimuovere se hai un file con questo nome altrove.
/qrcode.png

# PNG QR ad altissima risoluzione usati solo per stampa in cartoleria
# (rimangono locali, non servono nel deploy)
/images/qr-zefiro-di-oksi-3000.png
/images/qr-zefiro-di-oksi-3000-margins.png
